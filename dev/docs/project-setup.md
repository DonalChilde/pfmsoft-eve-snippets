# Project Setup from Template

## Edits

- AGENTS.md file from template.
  - set project name and entry point if any
- Rename src and test directories
- Fix directory references in .vscode/settings.json
- fix project name and directory references in pyproject.toml
- fix project references in `__init__.py`
- Optional do zensical setup
- Optional do pypi setup
